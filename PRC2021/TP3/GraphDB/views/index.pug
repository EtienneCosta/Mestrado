extends layout 
block content 
    .w3-card-4
        header.w3-container.w3-teal
            h3 Lista de Repositórios 
        if repos.length > 0
            .w3-container
                table(class="w3-table-all")
                    tr
                        th ID 
                        th Título
                        th URI
                    each r in repos 
                      tr 
                        td
                          a(href='/repositorio/'+r.id)= r.id
                        td= r.title
                        td= r.uri 
        else 
            .w3-container
              p Ainda não há repositórios criados ... 
        footer(class="w3-container w3-teal")
            address Gerado por galuno :: 2021-Março, Etienne Costa
                  a(href='/')  [Voltar] 

            